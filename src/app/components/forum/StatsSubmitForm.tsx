// src/app/components/forum/StatsSubmitForm.tsx
export default function StatsSubmitForm() {
  return (
    <form className="card form-grid">
      {/* TODO: Add stats submission fields (e.g., shots fired, kills) */}
      <p className="muted">Stats Submit Form Placeholder</p>
    </form>
  );
}
